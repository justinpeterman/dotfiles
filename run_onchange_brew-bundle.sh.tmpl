#!/usr/bin/env bash
# chezmoi re-runs this whenever Brewfile changes.
# hash: {{ include "Brewfile" | sha256sum }}
set -euo pipefail

echo "→ Running brew bundle..."
brew bundle --file="{{ .chezmoi.sourceDir }}/Brewfile"
echo "✓ brew bundle complete"
